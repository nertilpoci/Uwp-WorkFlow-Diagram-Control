<UserControl
    x:Class="WorkFlow.Controls.Workflow.WorkFlowEditor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WorkFlow.Controls.Workflow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" 
     xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"

    HorizontalAlignment="Stretch"  VerticalAlignment="Stretch"
   
   >

    <Grid Name="container"    HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" >

            <controls:DockPanel  LastChildFill="True" >
            <ScrollViewer ZoomMode="Enabled"  HorizontalScrollMode="Enabled" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" Name="scroller" Background="Red" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible">

                <Canvas Background="#EFEFEF" MinHeight="300" MinWidth="300"    x:Name="editorCanvas" ></Canvas>
        </ScrollViewer>
        </controls:DockPanel>


    </Grid>
</UserControl>
